#!/bin/bash

python3 -m venv venv
source venv/bin/activate
python3 -m pip install --upgrade pip
python3 -m pip install ansible molecule molecule-docker yamllint ansible-lint 
if [ -f ".token" ]; then
  export ACCESS_TOKEN=$(cat .token)
fi
